# Ambiente mínimo para rodar a suíte de testes:
#
#     pip install -r backend/requirements-dev.txt
#     pytest
#
# De propósito NÃO inclui `-r requirements.txt`: o easyocr arrasta o torch, e são ~2 GB
# para rodar teste que não toca em imagem nenhuma. Os módulos que dependem de OpenCV e do
# motor de OCR (services/mahu_ocr.py) só rodam no ambiente completo — é por isso que a
# lógica de parsing vive em services/mahu_parse.py, sem essas dependências.
#
# Para o ambiente completo (subir a API, avaliar OCR): pip install -r backend/requirements.txt
pytest==8.3.3
pydantic==2.9.0
psychrolib==2.5.0
# Só para os testes de API (backend/tests/test_api_processo.py). Continuam de fora o
# opencv-python-headless e o easyocr: são eles que pesam.
fastapi==0.115.0
aiosqlite==0.20.0
httpx==0.27.2
python-multipart==0.0.9
